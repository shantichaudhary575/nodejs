<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <form method="post" action="/saveEdited/<%=expense._id %>">
        <div class="form-group">
            <label for="name">Title:</label>
            <input type="string" class="form-control" id="title1" name="title"  value="<%= expense.title %>" placeholder="Enter title">
        </div>
        <div class="form-group">
            <label for="paidBy">Paid by:</label>
            <select class="form-control" id="paidBy1" value="<%= expense.paidBy %>" name="paidBy">
                <option>Aayesha</option>
                <option>Suvu</option>
                <option>Sonika</option>
              </select>
        </div>
        <div class="form-group">
            <label for="description">Description:</label>
            <input type="string" class="form-control" id="description" name="description"  value="<%= expense.description %>"
             placeholder="Enter description">
        </div>
        <div class="form-group">
            <label for="amount">Amount:</label>
            <input type="string" class="form-control" id="amount" name="amount"  value="<%= expense.amount %>" placeholder="Enter amount">
        </div>

        <button type="submit" class="btn btn-primary">Update</button>
    </form>
</body>
</html>